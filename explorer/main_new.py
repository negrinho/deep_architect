
namespace = {}

# this solution allows us to do pattern matching on the names.
# what are the problems with the current solution?

# for saving state, I could just lookup the values that need saving.
# it would take a bunch of state type strings.
# it makes more sense not to hide behind so many classes.


{
    "_REFERENCES_" : ["a.json", "b.json"],

}

# NOTE:  think that having the code in the right place.